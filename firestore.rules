rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WhatsApp Authentication and Session Documents
    match /wa_sessions/{sessionId} {
      allow read, write: if request.auth != null;
      
      match /keys/{keyId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
